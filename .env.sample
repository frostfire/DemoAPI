# Copy to .env and adjust if the defaults collide with something on your machine.
# These are local development values only - production secrets are injected on
# the server via env_file and never live in this repo.
POSTGRES_PORT=5433
POSTGRES_DB=caseflow
POSTGRES_USER=caseflow
POSTGRES_PASSWORD=caseflow_local_dev
